# Terminus Web

React + Vite wrapper for the original Terminus browser game.

## Development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
npm run preview
```

The original game scripts and assets are kept under `public/static` and loaded
in their original dependency order by the React application.
